{
    "name": "calmohallag/nullable-casting",
    "description": "PHP supports expression casting to primitive type (like int) by using “( type ) expression”, but it currently doesn't allow to use a nullable type as introduced by PHP 7.1 (e.g. ?int). This library provide functions that extend native php casting, adding nullable casting.",
    "type": "library",
    "license": "MIT",
    "authors": [
        {
            "name": "calmohallag",
            "email": "calmohallag@gmail.com"
        }
    ],
    "autoload": {
        "files": [
            "src/functions.php"
        ]
    },
    "autoload-dev": {
        "psr-4": {
            "Calmohallag\\NullableCasting\\Tests\\": "tests/"
        }
    },
    "require": {
        "php": ">=7.2"
    },
    "require-dev": {
        "phpunit/phpunit": "^9.4"
    }
}
